--- /Users/tron/work/go/pkg/mod/github.com/ipfs/go-ipfs-config@v0.0.3/init.go	2019-06-28 11:05:50.000000000 +0800
+++ init.go	2019-06-28 11:04:59.000000000 +0800
@@ -49,8 +49,8 @@
 
 		// setup the node mount points.
 		Mounts: Mounts{
-			IPFS: "/ipfs",
-			IPNS: "/ipns",
+			IPFS: "/btfs",
+			IPNS: "/btns",
 		},
 
 		Ipns: Ipns{
@@ -156,8 +156,8 @@
 		return ident, errors.New("bitsize less than 1024 is considered unsafe")
 	}
 
-	fmt.Fprintf(out, "generating %v-bit RSA keypair...", nbits)
-	sk, pk, err := ci.GenerateKeyPair(ci.RSA, nbits)
+	fmt.Fprintf(out, "generating %v-bit ECDSA keypair...", nbits)
+	sk, pk, err := ci.GenerateKeyPair(ci.ECDSA, nbits)
 	if err != nil {
 		return ident, err
 	}
